# Example 10: 自主学习 Agent - 竞技游戏 AI

## 复杂度
⭐⭐⭐⭐⭐⭐ 专家级（最高难度）

## 需求

### 背景
实现一个能够持续学习和自我进化的游戏 AI Agent，通过感知-决策-执行-反馈的循环，不断积累经验、优化策略，最终达到超越规则和初始设计的自主智能水平。这是 LangGraph 能力的终极展示。

### 功能需求

#### 1. 游戏场景定义

**选择游戏：简化版德州扑克（Texas Hold'em）**

原因：
- 有明确的规则和胜负判定
- 需要策略思考（概率计算、对手建模）
- 包含不确定性（牌的随机性）
- 有学习价值（从失败中优化策略）

游戏规则：
- 2名玩家
- 每人发2张底牌
- 5张公共牌分三轮发出（翻牌3张、转牌1张、河牌1张）
- 每轮可以：跟注、加注、弃牌
- 最后比牌面大小

#### 2. Agent 核心循环

```
┌──────────────────────────────────────────┐
│         持续学习循环（无限）              │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  1. 感知游戏状态                    │ │
│  │     - 手牌                         │ │
│  │     - 公共牌                       │ │
│  │     - 对手行为                     │ │
│  │     - 筹码情况                     │ │
│  └──────────┬─────────────────────────┘ │
│             │                            │
│             ▼                            │
│  ┌────────────────────────────────────┐ │
│  │  2. 制定策略                        │ │
│  │     - 计算胜率                     │ │
│  │     - 评估风险                     │ │
│  │     - 对手建模                     │ │
│  │     - 查询历史经验                 │ │
│  └──────────┬─────────────────────────┘ │
│             │                            │
│             ▼                            │
│  ┌────────────────────────────────────┐ │
│  │  3. 执行动作                        │ │
│  │     - 选择行动                     │ │
│  │     - 计算筹码                     │ │
│  └──────────┬─────────────────────────┘ │
│             │                            │
│             ▼                            │
│  ┌────────────────────────────────────┐ │
│  │  4. 观察结果                        │ │
│  │     - 对手反应                     │ │
│  │     - 这轮输赢                     │ │
│  └──────────┬─────────────────────────┘ │
│             │                            │
│             ▼                            │
│  ┌────────────────────────────────────┐ │
│  │  5. 存储记忆                        │ │
│  │     - 短期记忆：本局游戏            │ │
│  │     - 长期记忆：所有游戏经验        │ │
│  │     - 对手特征库                   │ │
│  └──────────┬─────────────────────────┘ │
│             │                            │
│             ▼                            │
│  ┌────────────────────────────────────┐ │
│  │  6. 策略优化                        │ │
│  │     - 分析失败原因                 │ │
│  │     - 更新策略权重                 │ │
│  │     - 发现新模式                   │ │
│  │     - 淘汰低效策略                 │ │
│  └──────────┬─────────────────────────┘ │
│             │                            │
│             └─────► 进入下一轮游戏       │
│                                          │
└──────────────────────────────────────────┘
```

#### 3. 详细模块设计

**模块1：感知系统（Perception）**

职责：全面感知当前游戏状态

输入：游戏环境
输出：结构化的状态表示

```python
class GameState:
    # 自己的信息
    my_hand: List[Card]  # 手牌
    my_chips: int        # 筹码
    my_position: str     # 位置（庄家/非庄家）
    
    # 公共信息
    community_cards: List[Card]  # 公共牌
    pot_size: int                # 底池
    current_bet: int             # 当前注额
    round_name: str              # 轮次（preflop/flop/turn/river）
    
    # 对手信息
    opponent_chips: int
    opponent_last_action: str  # fold/call/raise
    opponent_bet_amount: int
    
    # 历史信息
    betting_history: List[Action]
```

感知节点实现：
```python
def perceive(state):
    """感知游戏状态"""
    # 解析牌面
    hand_strength = calculate_hand_strength(state)
    
    # 计算胜率
    win_probability = monte_carlo_simulation(
        my_hand=state["my_hand"],
        community=state["community_cards"],
        num_simulations=1000
    )
    
    # 对手建模
    opponent_profile = load_opponent_model(state["opponent_id"])
    
    return {
        "hand_strength": hand_strength,
        "win_probability": win_probability,
        "opponent_profile": opponent_profile,
        "perceived_state": state
    }
```

---

**模块2：策略制定（Strategy Planning）**

职责：基于当前状态和历史经验制定决策

策略类型：
1. **基础策略（Rule-based）**
   - 牌力阈值策略
   - 位置策略
   - 底池赔率计算

2. **经验策略（Experience-based）**
   - 查询相似历史局面
   - 使用向量数据库检索
   - 参考过去的成功/失败案例

3. **对手适应策略（Opponent Modeling）**
   - 激进型对手 → 更保守
   - 保守型对手 → 更激进
   - 诈唬频率分析

4. **动态策略（Dynamic）**
   - 根据筹码量调整
   - 根据游戏阶段调整
   - 根据学习到的模式调整

策略评分：
```python
def plan_strategy(state):
    """制定策略"""
    # 1. 基础评分
    base_score = evaluate_hand_strength(state)
    
    # 2. 经验加成
    similar_cases = query_memory(
        hand=state["my_hand"],
        community=state["community_cards"],
        opponent_action=state["opponent_last_action"],
        top_k=5
    )
    experience_adjustment = analyze_similar_cases(similar_cases)
    
    # 3. 对手建模
    opponent_model = state["opponent_profile"]
    opponent_adjustment = calculate_opponent_factor(opponent_model)
    
    # 4. 综合决策
    strategies = [
        {"action": "fold", "score": ...},
        {"action": "call", "score": ...},
        {"action": "raise_small", "score": ...},
        {"action": "raise_large", "score": ...},
        {"action": "all_in", "score": ...},
    ]
    
    # 添加探索因子（避免陷入局部最优）
    if random.random() < state["exploration_rate"]:
        return random.choice(strategies)
    
    return max(strategies, key=lambda x: x["score"])
```

---

**模块3：动作执行（Action Execution）**

职责：将策略转化为具体行动

```python
def execute_action(state):
    """执行动作"""
    action = state["planned_action"]
    
    # 执行并获取结果
    result = game_engine.take_action(
        player_id=state["my_id"],
        action=action["action"],
        amount=action.get("amount", 0)
    )
    
    return {
        "action_taken": action,
        "game_state_after": result["new_state"],
        "opponent_response": result["opponent_action"]
    }
```

---

**模块4：结果观察（Observation）**

职责：观察行动的即时和长期结果

```python
def observe_result(state):
    """观察结果"""
    observations = {
        "immediate": {
            "opponent_response": state["opponent_response"],
            "pot_change": calculate_pot_change(state),
            "position_change": evaluate_position(state),
        },
        "round_end": None,  # 如果这轮结束
        "game_end": None,   # 如果整局结束
    }
    
    # 如果整局结束
    if state["game_ended"]:
        observations["game_end"] = {
            "result": "win" if state["i_won"] else "lose",
            "final_pot": state["final_pot"],
            "showdown": state["cards_revealed"],
        }
    
    return observations
```

---

**模块5：记忆存储（Memory Storage）**

职责：多层次记忆系统

**短期记忆（Working Memory）**
- 范围：当前这一局游戏
- 内容：所有轮次的决策和结果
- 用途：本局内的对手建模

**长期记忆（Long-term Memory）**
- 范围：所有历史游戏
- 存储方式：向量数据库（如 Chroma、Pinecone）
- 检索方式：语义相似度搜索

**对手特征库（Opponent Profiles）**
- 每个对手的行为特征
- 统计数据：
  - 诈唬频率
  - 激进度
  - 弃牌阈值
  - 常用模式

**策略知识库（Strategy Library）**
- 成功策略集合
- 每个策略的成功率
- 适用场景标记

实现：
```python
def store_memory(state):
    """存储记忆"""
    # 1. 短期记忆（本局）
    state["working_memory"].append({
        "round": state["round_number"],
        "action": state["action_taken"],
        "result": state["observations"],
        "timestamp": time.time()
    })
    
    # 2. 长期记忆（如果游戏结束）
    if state["game_ended"]:
        game_summary = create_game_summary(state)
        
        # 向量化并存储
        embedding = create_embedding(game_summary)
        vector_db.add(
            embedding=embedding,
            metadata=game_summary,
            id=f"game_{state['game_id']}"
        )
        
        # 3. 更新对手档案
        update_opponent_profile(
            opponent_id=state["opponent_id"],
            game_data=game_summary
        )
        
        # 4. 更新策略评分
        update_strategy_scores(
            strategies_used=state["strategies_used"],
            game_result=state["result"]
        )
    
    return state
```

---

**模块6：策略优化（Strategy Optimization）**

职责：从经验中学习，优化决策模型

学习机制：

**A. 经验回放（Experience Replay）**
```python
def experience_replay(state):
    """从历史经验中学习"""
    # 随机采样历史游戏
    past_games = sample_from_memory(n=50)
    
    for game in past_games:
        # 重新评估过去的决策
        for decision in game["decisions"]:
            # 用当前知识重新评分
            current_score = evaluate_with_current_model(decision)
            past_score = decision["original_score"]
            
            # 如果评分提升，说明学到了新东西
            if abs(current_score - past_score) > threshold:
                # 更新模型
                update_model(decision, current_score)
```

**B. 模式发现（Pattern Discovery）**
```python
def discover_patterns(state):
    """发现新模式"""
    # 聚类分析历史决策
    decision_clusters = cluster_decisions(
        all_decisions=load_all_decisions(),
        method="k-means",
        n_clusters=10
    )
    
    # 分析每个聚类的特征
    for cluster in decision_clusters:
        pattern = {
            "typical_situation": extract_features(cluster),
            "best_action": find_most_successful_action(cluster),
            "success_rate": calculate_success_rate(cluster),
        }
        
        # 如果发现高成功率的新模式
        if pattern["success_rate"] > 0.7:
            add_to_strategy_library(pattern)
```

**C. 策略淘汰（Strategy Pruning）**
```python
def prune_strategies(state):
    """淘汰低效策略"""
    for strategy in state["strategy_library"]:
        # 计算最近表现
        recent_performance = evaluate_recent_performance(
            strategy=strategy,
            last_n_games=100
        )
        
        # 如果持续表现不佳，降低权重或删除
        if recent_performance < threshold:
            strategy["weight"] *= 0.8  # 降权
            
        if strategy["weight"] < 0.1:
            remove_strategy(strategy)  # 淘汰
```

**D. 参数调优（Hyperparameter Tuning）**
```python
def optimize_parameters(state):
    """优化关键参数"""
    parameters = {
        "exploration_rate": state["exploration_rate"],
        "risk_tolerance": state["risk_tolerance"],
        "bluff_frequency": state["bluff_frequency"],
        # ... 更多参数
    }
    
    # 基于最近表现调整
    recent_win_rate = calculate_recent_win_rate(last_n=100)
    
    if recent_win_rate < 0.4:
        # 表现不佳，增加探索
        parameters["exploration_rate"] *= 1.2
    elif recent_win_rate > 0.6:
        # 表现很好，减少探索，强化利用
        parameters["exploration_rate"] *= 0.9
    
    return parameters
```

#### 4. 状态管理

```python
class AgentState(TypedDict):
    # 游戏状态
    game_state: GameState
    game_id: str
    game_number: int  # 第几局游戏
    
    # 感知
    perceived_state: dict
    hand_strength: float
    win_probability: float
    
    # 策略
    available_strategies: list
    planned_action: dict
    
    # 执行
    action_taken: dict
    opponent_response: dict
    
    # 观察
    observations: dict
    
    # 记忆
    working_memory: list  # 短期
    long_term_memory_ref: str  # 长期记忆引用
    
    # 学习
    strategies_used: list
    strategy_scores: dict
    exploration_rate: float
    
    # 对手建模
    opponent_id: str
    opponent_profile: dict
    
    # 性能指标
    total_games_played: int
    win_rate: float
    avg_profit: float
    learning_progress: dict
```

#### 5. 完整流程图

```python
from langgraph.graph import StateGraph, END

workflow = StateGraph(AgentState)

# 添加节点
workflow.add_node("perceive", perceive_node)
workflow.add_node("plan_strategy", strategy_node)
workflow.add_node("execute_action", execute_node)
workflow.add_node("observe_result", observe_node)
workflow.add_node("store_memory", memory_node)
workflow.add_node("optimize_strategy", optimize_node)

# 添加边
workflow.set_entry_point("perceive")
workflow.add_edge("perceive", "plan_strategy")
workflow.add_edge("plan_strategy", "execute_action")
workflow.add_edge("execute_action", "observe_result")
workflow.add_edge("observe_result", "store_memory")

# 条件边：决定是否继续游戏
workflow.add_conditional_edges(
    "store_memory",
    decide_next,
    {
        "continue_game": "perceive",      # 继续本局
        "optimize": "optimize_strategy",  # 本局结束，优化
        "end": END                        # 全部结束
    }
)

workflow.add_conditional_edges(
    "optimize_strategy",
    decide_after_optimize,
    {
        "play_again": "perceive",  # 开始新一局
        "end": END                 # 结束
    }
)

app = workflow.compile()
```

### 技术要求

#### LangGraph 学习点
- 掌握无限循环的 Agent 设计
- 理解长期记忆的集成（向量数据库）
- 学习策略的动态更新
- 掌握并行执行（多个游戏同时进行）
- 理解强化学习与 LangGraph 的结合
- 学习自适应参数调整

#### 实现要点

1. **向量数据库集成**：
```python
from langchain.vectorstores import Chroma
from langchain.embeddings import OpenAIEmbeddings

memory_db = Chroma(
    collection_name="poker_experiences",
    embedding_function=OpenAIEmbeddings()
)

def query_similar_situations(current_state):
    query_text = f"Hand: {current_state['hand']}, Community: {current_state['community']}"
    results = memory_db.similarity_search(query_text, k=5)
    return results
```

2. **策略权重更新**：
```python
def update_strategy_weights(strategy_id, result):
    """根据结果更新策略权重"""
    strategy = load_strategy(strategy_id)
    
    # 使用指数移动平均
    alpha = 0.1
    if result == "win":
        strategy["weight"] = (1 - alpha) * strategy["weight"] + alpha * 1.0
    else:
        strategy["weight"] = (1 - alpha) * strategy["weight"] + alpha * 0.0
    
    save_strategy(strategy)
```

3. **并行游戏**：
```python
# 同时运行多个游戏加速学习
async def parallel_training(n_games=100):
    tasks = []
    for i in range(n_games):
        task = app.ainvoke({"game_id": f"game_{i}"})
        tasks.append(task)
    
    results = await asyncio.gather(*tasks)
    return results
```

### 验收标准

1. **学习能力**
   - 胜率随游戏局数提升
   - 能发现并利用对手弱点
   - 策略库不断丰富

2. **适应能力**
   - 能够适应不同风格的对手
   - 在不同筹码量下调整策略
   - 根据局势变化动态调整

3. **记忆系统**
   - 能够检索相似历史局面
   - 记忆不会无限增长（有淘汰机制）
   - 检索速度快（<100ms）

4. **性能提升**
   - 前100局 vs 后100局胜率对比
   - 对抗规则型 AI 的胜率
   - 平均每局收益提升

### 示例输入输出

**训练过程演示**

```
==========================================
初始化 Agent
==========================================
[系统初始化]
✓ 创建向量数据库
✓ 初始化策略库（10个基础策略）
✓ 设置探索率：0.3
✓ 加载对手数据库

准备开始训练...

==========================================
第 1 局游戏
==========================================
对手: RandomBot（随机AI）
初始筹码: 1000

--- 第1轮: Pre-flop ---
[感知]
我的手牌: ♠A ♥K (强牌)
位置: 庄家
对手行为: Call

[策略制定]
查询历史经验: 没有相关经验（第一局）
基础评分: 8.5/10
决策: Raise 50

[执行]
✓ 加注 50
底池: 110

[观察]
对手反应: Call（跟注）
→ 推测: 对手可能有中等牌力

--- 第2轮: Flop ---
公共牌: ♠K ♦7 ♣2 (我有一对K)

[感知]
牌力: 顶对顶踢脚
胜率估算: 82% (蒙特卡洛1000次模拟)

[策略]
决策: Raise 100

[执行]
✓ 加注 100

[观察]
对手: Fold（弃牌）
→ 我赢得底池 110

--- 游戏结束 ---
结果: ✅ 胜利
收益: +60 筹码
最终筹码: 1060

[存储记忆]
✓ 记录这局游戏
✓ 更新对手档案: RandomBot - 容易弃牌
✓ 策略"强牌激进"表现良好 → 权重 +0.1

==========================================
第 50 局游戏（学习中）
==========================================
对手: AggressiveBot（激进AI）
当前筹码: 1420

--- Pre-flop ---
我的手牌: ♥9 ♦8 (中等牌)

[感知]
对手: AggressiveBot
对手特征（从记忆加载）:
  - 诈唬频率: 45% (非常高)
  - 激进度: 85%
  - 弃牌率: 15% (很少弃牌)

[策略]
查询历史: 找到3次类似情况
  - 案例1: 对抗激进对手，保守跟注，后来赢了
  - 案例2: 诈唬失败，损失200
  - 案例3: 弃牌避免大损失

综合决策: Call（跟注，看公共牌）
原因: 对手经常诈唬，值得看看

[执行]
✓ 跟注

--- Flop ---
公共牌: ♥10 ♦J ♠Q (我有顺子!)

[感知]
牌力: 顺子
胜率: 95%

[策略]
对抗激进对手策略: 诱敌深入
决策: Check（过牌）
预期: 对手会大注诈唬

[执行]
✓ 过牌

[观察]
对手: All-in 500！（果然诈唬）

[策略重新评估]
我有坚果牌（最强），立即跟注
决策: Call All-in

[执行]
✓ 跟注

--- Showdown ---
对手牌: ♣7 ♠3 (什么都没有，纯诈唬)
结果: ✅ 我赢！
收益: +500
最终筹码: 1920

[记忆更新]
✓ 强化策略: "对抗激进对手时，有好牌可诱敌"
✓ 策略权重大幅提升
✓ 对手建模准确性 +5%

==========================================
第 100 局游戏（已有经验）
==========================================
当前筹码: 2150
累计胜率: 58%

[策略库状态]
- 总策略数: 23个（新学习了13个）
- 顶级策略:
  1. "强牌激进" (权重: 0.92)
  2. "对抗激进对手诱敌" (权重: 0.88)
  3. "位置优势利用" (权重: 0.85)
- 淘汰策略: 5个低效策略已移除

[对手数据库]
- RandomBot: 85%胜率
- AggressiveBot: 62%胜率
- ConservativeBot: 71%胜率
- SmartBot: 45%胜率（需要更多学习）

--- 游戏进行 ---
[使用优化后的策略，表现出色]

==========================================
第 500 局游戏（高级阶段）
==========================================
当前筹码: 4850
累计胜率: 67%

[新发现的模式]
模式#12: "三次小注诱导诈唬"
  - 适用场景: 有强牌，对手激进
  - 成功率: 78%
  - 发现于: 第287-350局之间
  - 状态: 已添加到策略库

[自我优化]
探索率: 0.3 → 0.12（减少随机性，更多利用已学知识）
风险容忍度: 动态调整（根据筹码量）

[对手建模升级]
现在能识别7种对手类型:
1. 完全随机型
2. 激进型
3. 保守型
4. 紧凶型
5. 松弱型
6. 位置型（根据位置改变策略）
7. 适应型（也在学习）

[性能对比]
前100局平均收益: +1.5 筹码/局
第400-500局平均收益: +8.7 筹码/局
提升: 5.8倍！

==========================================
第 1000 局 - 里程碑
==========================================
总游戏数: 1000局
总胜率: 71%
最终筹码: 12,400 (12.4倍起始筹码)

[学习成果总结]

策略进化:
- 初始: 10个规则策略
- 现在: 47个策略（37个自主学习）
- 策略树深度: 3层

记忆系统:
- 存储游戏: 1000局
- 存储决策点: 18,756个
- 向量检索平均耗时: 45ms
- 记忆命中率: 82%

对手建模:
- 对抗过的对手: 15个
- 识别的行为模式: 67种
- 平均建模准确度: 78%

创新发现:
发现了3个超越初始规则的策略:
1. "动态筹码比例下注"（传统AI少用）
2. "多轮小注心理战"（需要对手建模）
3. "位置-牌力联合评估"（复杂决策树）

与传统AI对比:
- vs 规则型AI: 83%胜率
- vs 传统机器学习AI: 58%胜率
- vs 人类新手: 估计75%胜率

[能力评估]
✅ 已掌握: 基本策略、概率计算
✅ 已掌握: 对手建模、适应性调整
✅ 已掌握: 经验学习、模式发现
🔄 学习中: 复杂心理博弈
🔄 学习中: 多对手同时建模

状态: 🎓 已达到中级偏上水平
建议: 继续训练对抗更强AI
```

### 扩展思考

完成基础需求后，可以考虑：

1. **更复杂的游戏**
   - 多人德州扑克（3-9人）
   - 其他游戏（象棋、围棋）
   - 实时战略游戏

2. **高级学习算法**
   - 集成强化学习（DQN、PPO）
   - 自对弈训练（AlphaGo 模式）
   - 课程学习（从简单对手到困难对手）

3. **元学习能力**
   - 学习如何学习
   - 快速适应新对手（Few-shot）
   - 迁移学习到新游戏

4. **多Agent协作**
   - 团队游戏（如桥牌）
   - Agent间知识共享
   - 集体智能涌现

5. **可解释性**
   - 决策过程可视化
   - 策略解释生成
   - 学习路径追踪

6. **性能优化**
   - 并行游戏训练
   - 分布式记忆系统
   - GPU加速推理

7. **对抗训练**
   - 对抗性对手生成
   - 发现Agent弱点
   - 持续安全性测试

8. **人机交互**
   - 向人类玩家学习
   - 解释给人类听
   - 教学模式

### 总结

这是 LangGraph 的终极应用场景，展示了：
- ✅ 复杂循环和状态管理
- ✅ 长期记忆和知识积累
- ✅ 自主学习和策略优化
- ✅ 动态适应和模式发现
- ✅ 多层决策和元认知

这个项目综合运用了前面9个示例的所有技术，代表了 LangGraph 构建自主智能系统的完整能力。
